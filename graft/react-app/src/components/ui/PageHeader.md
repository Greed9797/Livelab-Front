# react-app/src/components/ui/PageHeader.tsx

- PageHeader · function · L3-L34 — function PageHeader({ eyebrow, title, accent, subtitle, actions, }: { eyebrow?: string title: string accent?: string subtitle?: string actions?: ReactNode })
