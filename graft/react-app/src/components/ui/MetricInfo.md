# react-app/src/components/ui/MetricInfo.tsx

- isMetricInfoCloseKey · function · L23-L25 — function isMetricInfoCloseKey(key: string): boolean
- MetricInfoPopover · function · L27-L68 — function MetricInfoPopover({ id, metric, detail, align = 'left', }: { id: string metric: MetricKey /** Memória de cálculo com os números REAIS do período, quando o payload já traz. */ detail?: string align?: 'left' | 'right' })
- MetricInfo · function · L70-L121 — function MetricInfo({ metric, detail, align = 'left', }: { metric: MetricKey detail?: string align?: 'left' | 'right' })
- onKeyDown · function · L86-L91 — function onKeyDown(event: KeyboardEvent)
